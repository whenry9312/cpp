#include <iostream>
#include <cmath>
using namespace std;
main(){
	int x,y;
	cin>>x;
	for(y=x-1;y>=1;y--){
		if ((y%4==0 && y%100!=0) || (y%400==0)){
			cout<<y<<' ';
			break; 
		}
	}
	for(y=x+1;y<=x+200;y++){
		if ((y%4==0 && y%100!=0) || (y%400==0)){
			cout<<y<<' ';
			break;
		} 
	}
}
